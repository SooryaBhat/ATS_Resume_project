# TalentMatch AI — backend + legacy Streamlit dependencies.
#
# Install with:
#     pip install -r requirements.txt
#
# After installing, the spaCy model is automatically loaded via wheel or:
#     python -m spacy download en_core_web_sm
#
# WeasyPrint (used for PDF export) also needs system libraries on Linux:
#     sudo dnf install -y cairo pango gdk-pixbuf2 libffi    # Fedora
#     sudo apt install -y libcairo2 libpango-1.0-0 libpangoft2-1.0-0 libffi-dev  # Debian/Ubuntu

# ── Backend: web framework ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
python-dotenv>=1.0

# ── Backend: NLP / ML ──
spacy==3.7.5
thinc==8.2.3
blis==0.7.11
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
scikit-learn>=1.4
numpy>=1.26
rapidfuzz>=3.6

# ── Backend: file parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27

# ── Backend: PDF report generation ──
jinja2>=3.1
weasyprint>=60.0

# ── Backend: external AI ──
google-generativeai>=0.8     # Google Gemini API (replaces Groq)

# ── Backend: external services ──
httpx>=0.27                  # async Supabase REST client
supabase>=2.4                # Supabase Python SDK (kept per project requirement)
PyJWT[crypto]>=2.8           # verifies Supabase access tokens (HS256)

# ── Legacy Streamlit frontend (preserved in legacy_streamlit/) ──
streamlit>=1.32
requests>=2.31

reportlab==4.2.2